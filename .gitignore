# --- Claude Code runtime / home state — NEVER track (contains secrets) ---
.credentials.json
.claude.json
.claude/
sessions/
projects/
history.jsonl
cache/
backups/
plugins/

# kern daemon data store (binary LMDB)
.kern/

# lock files
*.lock
*.lock.json
