# Terms

The canonical names of the machine. This file is the **rename registry**, not a
dictionary — it exists so the main terms can be renamed in one clean grep-driven
sweep. Full definitions live once in `/.machine/glossary.csv` (the single source);
do not restate them here.

Each row: the canonical spelling, its kind, a one-line role, and where it lives.
To rename a term, change it here, in the glossary, and grep the codebase for the
old name.

## Core identity

| Term | Kind | Role | Lives in |
|------|------|------|----------|
| the machine | identity | The portable, project-agnostic Claude Code setup | `.claude/` |
| the portable machine | layer | Identical-in-every-repo payload (skills/agents/hooks/rules) | `.claude/` |
| the project layer | layer | Per-repo specialization regenerated by `/oil` | `/.machine/` |
| kern | daemon | Durable per-cwd memory daemon (`mcp__kern__*`) | `.kern/` |
| mesh | daemon | Per-cwd fleet-coordination daemon (`mcp__mesh__*`) | `.mesh/`, `mesh/` |
| git-fs | layer | Branch-per-agent isolation + change history (`agent/<id>`) | `.git-fs/` |

## Orchestration (the drill surface)

| Term | Kind | Role | Lives in |
|------|------|------|----------|
| the drill | surface | Grill-first main-loop driver (agent + skill) | `.claude/skills/drill/`, `.claude/agents/drill.md` |
| grill-first | mode | Refine a request one question at a time until valid before any spawn | `.claude/skills/drill/` |
| miner | surface | Spoken name for a dispatched implementation sub-agent | `.claude/skills/drill/` |
| plan agent | role | Sub-agent that writes the implementation plan | `.claude/skills/drill/` |
| implementation agent | role | Sub-agent that builds on its own git-fs branch | `.claude/skills/drill/` |
| ledger | artifact | Live roster of per-job entry-files (the set of files IS the roster) | `/.machine/sessions/` |
| gate one / gate two | gates | Human approval before implement / before merge | `.claude/skills/drill/` |
| board-trust | guard | Only driver-owned ledger entries may be acted on | `.claude/skills/drill/` |
| factory agent | role | Default agent owning one feature's full eight-stage lifecycle | `target.md` |

## Process skills

| Term | Kind | Role | Lives in |
|------|------|------|----------|
| /oil | skill | Re-index `/.machine/`; specialize the machine to this repo | `.claude/skills/oil/` |
| /mine | skill | Survey the mine graph + repo, slot best-fit addons | `.claude/skills/mine/` |
| /gate | skill | Format + lint + test + build pass/fail gate | `.claude/skills/gate/` |
| /personas | skill | Parallel persona review panel | `.claude/skills/personas/`, `/.machine/personas/` |
| /improve | skill | Rate-and-improve sweep driven by `improve.json` | `.claude/skills/improve/` |
| codex-review | skill | Advisory second-AI pass (never gates) | `.claude/skills/codex-review/` |

## Law & vocabulary

| Term | Kind | Role | Lives in |
|------|------|------|----------|
| Machine law | law | Always-applies rules (root cause, one clean impl, glossary discipline) | `.claude/agents/default.md` |
| Project law | law | Per-repo binding rules | `/.machine/agent.md` |
| glossary | artifact | Single source for full term definitions | `/.machine/glossary.csv` |
| mine graph | artifact | Curated kit of unregistered addons | `mine/` |
| improve.json | artifact | Outstanding-work list for the `/improve` loop | `/.machine/improve.json` |
